= simple_form_for @idea, url: wizard_path do |f|
  = yield f

  %a{href: next_wizard_path}
    %button{class: 'green-button'}
      = t('ideas._form.button.skip')

  = f.submit t('ideas._form.button.continue'), class: 'green-button'
