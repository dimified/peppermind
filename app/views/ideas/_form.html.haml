= simple_form_for @idea, defaults: { input_html: { class: 'styled_input' } } do |f|
  = f.input :question, placeholder: t('ideas._form.placeholder.question')
  = f.input :description, as: :text, placeholder: t('ideas._form.placeholder.description')
  = f.input :duration, input_html: {class: 'styled_input datepicker'}, placeholder: 'Dauer'

  = f.submit t('ideas._form.button.create'), class: 'btn green-button'
  = link_to t('ideas._form.button.cancel'), root_path, class: 'btn red-button'
