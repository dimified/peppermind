:ruby
  content_for :stylesheets do
    stylesheet_link_tag "landingpage"
  end

.gradient
  .logo
  .container
    .login
      - if user_signed_in?
        = link_to 'Abmelden', destroy_user_session_path, method: :delete
      - else
        = render 'users/login', resource: :user
