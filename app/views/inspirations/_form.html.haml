= simple_form_for([@challenge, @inspiration]) do |f|
  = f.input :description, as: :text, placeholder: t('challenge_inspirations._form.placeholder.description')
  = f.submit t('challenge_inspirations._form.button.submit')

