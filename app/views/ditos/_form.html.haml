#dito-form
  - if dito = current_user.ditos.where(challenge: @challenge).first
    = simple_form_for(dito, remote: true, method: :delete) do |f|
      = hidden_field_tag('challenge_id', @challenge.id)
      = f.submit "Undito"
  - else
    = simple_form_for(current_user.ditos.build(challenge: @challenge), remote: true) do |f|
      = hidden_field_tag('challenge_id', @challenge.id)
      = f.submit "Dito"
