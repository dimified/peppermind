$('#activities').append('<%= escape_javascript(render(@activities)) %>');
<% if @activities.current_page < @activities.num_pages %>
$('.pagination').replaceWith('<%= j paginate(@activities) %>');
<% else %>
$('.pagination').remove();
<% end %>
