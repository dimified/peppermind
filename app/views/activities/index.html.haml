- content_for :javascript do
  = javascript_include_tag 'endless_scroll'

= title t('.title')
= link_to new_challenge_path, { class: 'btn title', id: 'new-challenge' } do
  .icon-plus
= link_to t('.link.all_challenges'), challenges_path, class: 'btn title', id: 'all-challenge'

#activities
  = render @activities

= paginate @activities
