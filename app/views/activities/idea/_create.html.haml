#{l(activity.created_at)}:
= link_to(activity.user.display_name, activity.user)
- if idea
  = t('activities.idea._create.exists', link: link_to(idea.title, idea)).html_safe
- else
  = t('activities.idea._create.deleted')
